Custom Hats

Noob Hat source image: noob_hat.png
Item ID: 600
Native renderer sprite is packed into tiles.png at (276,528), so the hat renders through the normal wearable system instead of the HTML overlay.
